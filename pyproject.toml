[build-system]
requires = [
    "setuptools>=69.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "ansible-directory-helper"
version = "0.3.1"
authors = [
    {name="Belal Ibrahim", email="belal.ibrahim@proton.me"}
]
description = "A small helper library to manage local directories for Ansible Runner"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml",
    "ansible-core>=2.18",
]
classifiers = [
    "Development Status :: 7 - Inactive",
    "Environment :: Console",
    "Framework :: Ansible",
    "Programming Language :: Python :: 3",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: Apache Software License",
]

[project.urls]
Homepage = "https://github.com/belal-i/ansible-directory-helper"
Issues = "https://github.com/belal-i/ansible-directory-helper/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools_scm]
